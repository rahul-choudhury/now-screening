{
  "name": "now-screening",
  "private": true,
  "description": "Connects Letterboxd with BookMyShow",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "cd apps/api && go run ./cmd/api",
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down"
  },
  "devDependencies": {
    "chrome-types": "^0.1.354"
  }
}
